# Engineer capability test

A tiny dependency-free Node.js HTTP server for the AI Actually engineering capability test.

## Run locally

```sh
npm start
```

The server uses `PORT` when provided and otherwise listens on port 3000. Visit
`http://localhost:3000/` for the demo or `http://localhost:3000/health` for its
health response.

## Test

```sh
npm test
```
