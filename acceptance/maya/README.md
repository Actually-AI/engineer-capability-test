# Maya acceptance demo

A dependency-free Node HTTP demo with a small browser form and JSON endpoints.

## Run

From this directory:

```sh
npm start
```

Then open <http://127.0.0.1:3000>. Set `PORT` to use a different port.

## Test

```sh
npm test
```

The test suite uses Node's built-in test runner and requires no install step.
